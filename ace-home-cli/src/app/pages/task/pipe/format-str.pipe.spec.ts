import { FormatStrPipe } from './format-str.pipe';

describe('FormatStrPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStrPipe();
    expect(pipe).toBeTruthy();
  });
});
